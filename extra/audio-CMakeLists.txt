#
# Audio Library
#
# <project>/src/audio
#

# Make an automatic library - will be static or dynamic based on user setting
add_library(audio dsp.cpp audiotx.cpp)

# We need this directory, and users of our library will need it too
target_include_directories(audio PUBLIC ${pierre_SOURCE_DIR}/include/pierre)

target_link_libraries(audio PRIVATE
  ALSA::ALSA
  ${Boost_LIBRARIES}
  Threads::Threads
)

target_compile_options(audio PRIVATE ${CXX_FLAGS})
